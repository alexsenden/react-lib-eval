<div style={{display: 'flex', flexDirection: 'column'}}>
  <Text type=${"title"} content=${"Your Progress"} />
  <ProgressBar progress=${tasksCompleted} totalSteps=${10} argCurrentStep=${tasksCompleted} />
</div>